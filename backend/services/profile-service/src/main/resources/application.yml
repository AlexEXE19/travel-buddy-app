spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/profile_db
    username: postgres
    password: ctipass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect